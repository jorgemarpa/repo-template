# It is important to store the version number in a separate file
# so that we can read it from setup.py without importing the package
# Keep track of this version with the pyproject.toml version
__version__ = "0.1.0"
